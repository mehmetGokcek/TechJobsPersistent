@model Employer

<h1>Employer: @Model.Name</h1>

<table class="table">
    <tr>
        <th>Name</th>
        <th>Location</th>

    </tr>
    <tr>
        <td>@Model.Name</td>
        <td>@Model.Location</td>
        <td>
            <button type="button" class="btn btn-danger"> <a class="text-white" asp-controller="Employer" asp-action="Delete" asp-route-id="@Model.Id">Delete</a> </button>
        </td>
    </tr>


</table>